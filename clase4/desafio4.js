//Desafio extra (Opcional)
//Aterrizar avión
let velocidad = 2700;
let altura = 200;
if ((velocidad >= 268 && velocidad <= 278) && (altura >= 150 && altura <= 300)) {
    console.log('el avión SI está listo para iniciar el aterrizaje');
} else {
    console.log('el avión NO está listo para iniciar el aterrizaje');
}